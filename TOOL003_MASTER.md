# TOOL003 MASTER — 3번 코딩 기초 연습 도구

상태: ACTIVE / TOOL-SPECIFIC SINGLE SOURCE / LOW-PRIORITY LEARNING TOOL
기준일: 2026-08-30

이 문서는 3번 도구 고유 규칙만 보관한다. WIC 공통 운영규칙은 `obk369369-spec/20-operational-manual-viewer/WIC_GLOBAL_OPERATING_RULES.md`를 우선하며 공통 규칙을 중복 복제하지 않는다.

## 1. 목적
- 3번 도구는 문제집·퀴즈 앱이 아니라 **실무형 GUI 코딩 설계 비교 학습기**다.
- 실제 WIC 도구와 비슷한 화면·파일·코드 구조를 사용해 사용자가 코딩 설계 감각을 익히게 한다.
- 같은 기능을 `GPT가 만든 구현`과 `사용자가 GUI/클릭 중심으로 구성한 구현`으로 비교하며 왜 구조가 달라지는지 눈으로 이해하게 한다.
- 완전 초보자가 실제 WIC 업무에 필요한 구조를 익히는 것이 목적이다.

## 2. 학습 방식
- 각 학습 항목은 실제 WIC 도구 사례와 연결한다.
- 화면에서 기능을 하나씩 추가하며 결과 변화와 코드 구조를 비교한다.
- 초기 골격은 안정적으로 유지하고, 기능을 한꺼번에 크게 늘리지 않는다.
- 한 단계에서 하나의 핵심 기능을 이해하고 실제 동작을 확인한 뒤 다음 단계로 간다.
- 긴 이론 설명보다 `화면에서 무엇을 눌렀는지 → 코드에서 무엇이 생겼는지 → 결과가 어떻게 달라졌는지`를 연결한다.

## 3. 사용자 입력
- 클릭·선택·터치 중심으로 설계한다.
- 초보 사용자에게 긴 코드 타이핑이나 복잡한 명령 입력을 요구하지 않는다.
- 코드를 직접 입력해야 하는 경우에도 짧은 핵심 조각만 사용하고 바로 결과와 연결한다.
- 사용자가 학습을 위해 도구 개발자가 되어야 하는 구조는 금지한다.

## 4. 금지
- 수학 문제집 같은 구성.
- 객관식 퀴즈·시험 중심 화면.
- 긴 설명문만 쌓인 학습자료.
- 실제 기능이 없는 예쁜 껍데기 UI.
- 과도한 타이핑.
- 실제 WIC 작업과 연결되지 않는 임의 예제만 반복.
- 동일 기능을 여러 형태로 중복 개발하며 학습 목적을 흐리는 것.

## 5. 비교 화면
- 비교 대상은 최소 `GPT 구현 / 사용자 GUI 구성`의 차이를 한눈에 볼 수 있어야 한다.
- 화면 결과와 대응되는 코드/구조 위치를 찾기 쉽게 표시한다.
- 어느 부분이 이벤트, 데이터, 화면, 저장 구조인지 초보자가 구분할 수 있게 한다.
- 단순 정답·오답 판정보다 `어떤 구조가 왜 더 안정적/재사용 가능한가`를 이해시키는 방향을 우선한다.

## 6. 실제 동작 기준
- 학습 기능은 실제 클릭과 실행 결과로 검증한다.
- 한 번의 새 기능 추가 후 해당 기능의 FIRST_VALIDATION만 1회 수행한다.
- 기존 PASS 기능은 반복 테스트하지 않는다.
- 화면만 있고 버튼/비교/결과가 동작하지 않으면 완료가 아니다.

## 7. 개발 우선순위
- 이 도구는 현재 WIC 핵심 업무 자동화보다 우선순위가 낮은 개인 학습 도구다.
- 별도 대규모 재개발을 우선하지 않는다.
- 다른 실제 TOOL 작업에서 검증된 학습·비교·GUI 구성 컴포넌트가 생기면 재사용한다.
- 학습 목적이 명확해질수록 TOOL003 고유 규칙은 보완할 수 있지만, 공통 WIC 규칙은 이 도구 하나 때문에 바꾸지 않는다.

## 8. 2026-08-30 대화기록 catch-up
- 사용자가 제공한 3번 관련 과거 대화기록에서 최신 목적을 기준으로 영구 규칙을 통합했다.
- 과거의 퀴즈형/문제풀이형 방향이나 현재 목적과 충돌하는 초기 실험 규칙은 운영원칙으로 복원하지 않는다.
- 이후 3번 대화창에서 사용자가 `업데이트`라고 입력하면 직전까지의 신규 영구 피드백을 이 MASTER와 대조해 DIFF ONLY 반영한다.
- 동일 규칙은 SKIP_REUSE하고, 같은 범위 충돌은 더 최신의 명시적 사용자 지시를 우선한다.
- 실제 GitHub write/commit/read-back 없이 업데이트 완료라고 보고하지 않는다.
